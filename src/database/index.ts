import { fetchAll } from "./fetchAll"
import { executeMultipleTransactions } from "./executeMultiTransaction"
import { executeTransaction } from "./executeTransaction"

export const database = {
    fetchAll,
    executeMultipleTransactions,
    executeTransaction
} 